# Third-party sources vendored for the Skia build. Never reformat these — the
# diff against their upstreams is the only way to audit them.
vendor/

# Build output and dependencies.
target/
node_modules/
lib/skia.node

# Test fixtures are compared byte-for-byte.
tests/assets/
