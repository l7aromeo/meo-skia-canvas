name: CI

# Runs on every pull request, which `test.yml` cannot: it rebuilds the native module from source
# across six OS/Node combinations, and a Skia build is far too slow to gate a review on.
#
# This fetches the prebuilt binary for the release instead, so a change is verified on Linux,
# macOS and Windows in about a minute each — the same rendering suite, against the same artifact
# users install. test.yml stays available for release-time verification that the module still
# builds from source.
on:
  # `paths-ignore` rather than `paths`: this job answers "does the shipped
  # binary still run this JavaScript", so it wants everything except the paths
  # that cannot change the answer. Prose and pictures cannot. `docs/api` is
  # listed too -- it documents the surface, it does not define it, and
  # rust-ci.yml watches the one page that feeds the crate's own docs.
  #
  # The pull request trigger no longer filters by path, and that is deliberate.
  # `main` carries a ruleset requiring checks, and a required check that never
  # reports is unmet rather than absent -- a path filter on the trigger means
  # no run exists at all, so nothing can report. Measured: PR #168 touched only
  # `Cargo.toml`, `THIRD-PARTY-NOTICES.md` and `justfile`, `docs.yml` produced
  # zero runs, and the pull request was blocked on a context from a workflow
  # that had correctly declined to exist.
  #
  # The saving the filter bought is kept by the `changes` job below: every
  # pull request instantiates this workflow, and the work skips when nothing
  # it covers has moved. A skipped job costs nothing and, unlike an absent
  # one, can be reported on.
  #
  # `push` keeps its filter: required checks are evaluated on pull requests,
  # so a skipped push run blocks nothing. Spelled out rather than shared,
  # because GitHub Actions does not resolve YAML anchors.
  pull_request:
  push:
    branches: [main]
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "LICENSE"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Which of this workflow's concerns actually moved. The list is the one the
  # trigger used to carry, in git's pathspec spelling rather than GitHub's:
  # `:(glob)` makes `*` stop at a directory separator, which is what makes
  # `*.md` mean a top-level Markdown file here exactly as it did there.
  # Verified against a tree holding `README.md`, `docs/guide.md`,
  # `docs/img/a.png`, `LICENSE`, `Cargo.toml` and `src/lib.rs`: the first four
  # are excluded and the last two are not.
  changes:
    name: changes
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      relevant: ${{ steps.scan.outputs.relevant }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - id: scan
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          # Only a pull request has a base to diff against. A push here has
          # already been filtered by the trigger, so anything reaching this
          # is relevant by construction.
          if [ -z "$BASE" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # The pathspecs live beside this workflow rather than in it, so the
          # check that guards them reads the same copy. `mapfile` keeps one
          # spec per element, which `$(...)` word-splitting would not.
          mapfile -t SPECS < <(grep -vE '^[[:space:]]*(#|$)' ".github/workflows/paths/ci.txt")
          moved=$(git diff --name-only "$BASE" "$HEAD" -- "${SPECS[@]}")
          if [ -n "$moved" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            echo "changed files this workflow covers:"
            echo "$moved"
          else
            echo "relevant=false" >> "$GITHUB_OUTPUT"
            echo "nothing this workflow covers has moved"
          fi

  test:
    needs: changes
    if: needs.changes.outputs.relevant == 'true'
    name: Test (${{ matrix.label }}, Node ${{ matrix.node }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        # Spelled out rather than crossed, because the two axes are not independent: Node version
        # is what varies on Linux, operating system is what varies everywhere else.
        #
        # Node 22 is the floor from `engines` and 24 the current active LTS; Node 20 reached
        # end-of-life on 2026-04-30, so testing it would only prove an unsupported runtime still
        # happens to work.
        #
        # macOS and Windows run one Node version each. What breaks per-platform in a native addon
        # is the binary and the loader, not the JavaScript, so a second Node version there costs a
        # job and finds nothing a Linux job would miss. Before this, the only automatic coverage
        # was Linux — Windows was tested solely when someone remembered to dispatch test.yml.
        include:
          - { os: ubuntu-latest, label: linux, node: 22 }
          - { os: ubuntu-latest, label: linux, node: 24 }
          - { os: macos-latest, label: macos, node: 24 }
          - { os: windows-latest, label: windows, node: 24 }

    # Windows runners default to PowerShell, where `if node ...; then` and `>> "$GITHUB_OUTPUT"`
    # are syntax errors. Git Bash ships on every runner image, so one setting keeps the steps
    # below identical across all three platforms.
    defaults:
      run:
        shell: bash

    steps:
      # Some test fixtures sit deeper than Windows' default 260-character path limit.
      - name: Enable long paths (Windows only)
        if: ${{ runner.os == 'Windows' }}
        run: git config --system core.longpaths true

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: latest

      # --ignore-scripts so the install step does not race the explicit fetch below.
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      # Needs no native binary, so it runs before the download and reports separately from a
      # rendering failure. Checked on every leg rather than once because the config pins
      # endOfLine, and Windows is the only runner that can catch a CRLF slipping in.
      - name: Check formatting
        run: bun run format:check

      # The declaration files are what the package ships as its `types`, and until this
      # existed nothing checked them -- `just typecheck` was Rust-only. Runs with
      # `skipLibCheck` off, which is how a consumer who disables it sees the package.
      # Needs no native binary, so it sits with the other pre-download checks.
      - name: Type-check the shipped declarations
        run: bun run typecheck

      # Download only, never `--or-compile`. Building Skia from source takes roughly an hour and
      # needs a toolchain this job deliberately does not install, so a compile fallback would turn
      # every pull request into an hour-long job rather than the minute this is meant to take.
      #
      # A missing asset is expected until the first release is published, and the suite below is
      # split so that gap does not silently become a pass.
      #
      # The root `install` script *is* `download --or-compile`, and that is correct where it lives:
      # a consumer installing on a platform with no published binary should compile rather than
      # fail. It does not reach this job, and has not since `--ignore-scripts` was restored to the
      # install step above. Those are two independent defences and neither relies on the other --
      # which is the point, because when the flag was missing the fallback compiled here and the
      # freshness check below then passed by answering about a binary this job had built itself.
      # Considered and deliberately left alone: the fix for that was the flag, not the script.
      - name: Fetch the native binary
        id: binary
        run: |
          if node lib/prebuild.mjs download; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "No published asset for v$(node -p "require('./package.json').version")."
          fi

      # Is there a published release for this version at all, or is one still on its way?
      #
      # `release-npm` bumps the version, tags it and opens a *draft* release, then dispatches
      # build.yml to fill it with binaries -- twenty minutes across seven platforms. The push of
      # that bump starts this workflow at the same moment, so for the whole of that window the
      # asset it wants does not exist yet and cannot: it is being compiled. Failing there reports
      # a release in progress as a broken build, on the one commit where nothing is wrong.
      #
      # Asked by tag, with the read-only token this workflow runs under: a draft release is not
      # reachable that way and answers 404, which is exactly the state to wait through. A 200
      # means the assets are published and a failed download is then a real fault.
      - name: Is a release published for this version?
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

          if gh api "repos/${GITHUB_REPOSITORY}/releases/tags/npm-v${VERSION}" >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "published=false" >> "$GITHUB_OUTPUT"

          # Which unpublished state, for the message below. A draft is not
          # reachable by tag, and whether it is listable at all depends on what
          # this token may see, so the answer is best-effort: "unknown" when the
          # listing is empty or refused, and the notice then says only what is
          # certain.
          #
          # Worth the extra call because the two states ask for different
          # things. No assets yet means wait for build.yml; assets built and the
          # release still a draft means run `just publish-npm` -- which is where
          # a release sat for an hour while later commits landed on main, under
          # a notice claiming the binaries were still compiling.
          assets=$(gh api "repos/${GITHUB_REPOSITORY}/releases" --paginate \
            --jq "[.[] | select(.tag_name == \"npm-v${VERSION}\" and .draft)][0].assets | length" \
            2>/dev/null || true)

          case "${assets:-}" in
            "" | null) echo "stage=unknown" >> "$GITHUB_OUTPUT" ;;
            0) echo "stage=building" >> "$GITHUB_OUTPUT" ;;
            *) echo "stage=drafted" >> "$GITHUB_OUTPUT"
               echo "assets=${assets}" >> "$GITHUB_OUTPUT" ;;
          esac

          echo "no published release for npm-v${VERSION} (draft assets: ${assets:-unknown})"

      # tests/static holds the suites that analyse files rather than exercise the binary, so they
      # are real signal even before the first release — and they are the only coverage the install
      # path has, which is where follow-redirects, https-proxy-agent and detect-libc are used.
      # Naming the directory rather than the files means a new static suite needs no edit here.
      # `tests/static/parity-npm.test.js` loads the npm extractor, which needs
      # the TypeScript pinned beside it: this directory is on 6.0.3 while the
      # root has moved to 7.x, whose point exports carry no compiler API. With
      # this directory uninstalled, Node resolution walks up to the root's and
      # the extractor throws on the first use of `ts.ScriptTarget`. Measured on
      # a cold checkout: 10 of 40 static tests fail that way, on every platform.
      #
      # Unconditional rather than the recipe's `test -d` guard, because CI is
      # always cold and the guard would only ever take the install branch. The
      # root install above cannot cover this -- hoisting the two together is
      # what the pin exists to prevent.
      - name: Install the pinned TypeScript the surface extractor needs
        run: bun install --cwd scripts/api-surface --frozen-lockfile

      - name: Test the packaging, install path and declarations
        run: bun run test:static

      # Was the binary fetched above built from this commit's source? This job asks "does what
      # users install work?", which is unanswerable once main moves past the published tag — and
      # answering it with a failure is what put every push after the first `src` change of a
      # cycle into the red, about 130 of them before v5.0.0, whatever the push touched.
      #
      # Compares `src` and `build.rs` only. `Cargo.toml` changes when a release bumps the version
      # without altering a byte of rendered output, so including it would mark the tree stale
      # after every release; `Cargo.lock` is untracked and cannot be compared at all. Dependency
      # drift with no `src` change therefore slips past this, which is what test.yml's weekly
      # from-source run exists to catch.
      #
      # One tag, fetched shallow. `fetch-depth: 0` on the checkout would pull the whole history
      # to answer a question about two paths.
      - name: Is the published binary built from this source?
        id: freshness
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if ! git fetch --depth=1 --no-tags origin \
                 "refs/tags/npm-v${VERSION}:refs/tags/npm-v${VERSION}" 2>/dev/null; then
            echo "current=unknown" >> "$GITHUB_OUTPUT"
            echo "no tag npm-v${VERSION} to compare against; treating the binary as current"
            exit 0
          fi

          moved=""
          for path in src build.rs; do
            if [ "$(git rev-parse "HEAD:${path}")" != "$(git rev-parse "npm-v${VERSION}:${path}")" ]; then
              moved="${moved}${path} "
            fi
          done

          if [ -n "$moved" ]; then
            echo "current=false" >> "$GITHUB_OUTPUT"
            echo "changed since npm-v${VERSION}: ${moved}"
          else
            echo "current=true" >> "$GITHUB_OUTPUT"
            echo "source matches npm-v${VERSION}"
          fi

      # Everything that actually renders. Whether not running is a failure
      # depends on why, and the three cases below decide it: a published
      # version whose binary could not be fetched is an error, because a run
      # that could not execute these has not verified them and a green check
      # saying otherwise is worse than a red one. The other two -- source that
      # has moved past the published binary, and a version whose release is
      # still a draft -- are notices, because neither is something a reader
      # could act on. Each says so at its own step.
      #
      # Pinned to the binary the step above fetched. The install step adds the optional platform
      # packages too, and those win over lib/skia.node during resolution — so without this the
      # suite would exercise a binary this job never checked, and the fetch step's success or
      # failure would have no bearing on what actually ran.
      - name: Test rendering
        if: steps.binary.outputs.available == 'true' && steps.freshness.outputs.current != 'false'
        env:
          MEO_SKIA_CANVAS_BINARY: ${{ github.workspace }}/lib/skia.node
        run: bun run test

      # Not a failure. The published binary predates this commit, so it cannot answer for it
      # either way, and a red check here would mean nothing a reader could act on. The engine is
      # still covered from source by rust-ci.yml on the same push; what goes uncovered until the
      # next release is the JavaScript binding against the new Rust.
      - name: Rendering tests skipped (source has moved)
        if: steps.binary.outputs.available == 'true' && steps.freshness.outputs.current == 'false'
        run: |
          echo "::notice::src or build.rs has changed since v$(node -p "require('./package.json').version"), so the published binary cannot verify this commit and the rendering suite did not run. rust-ci.yml covers the engine from source; the binding is verified at release by test.yml."

      # Nothing to verify against yet, and nothing wrong: the release for this version is still
      # a draft with its binaries in the oven. A notice rather than a failure, for the same
      # reason the moved-source case above is one -- neither is something a reader can act on.
      # Three unpublished states, and they ask for different things. Naming the
      # wrong one is how this notice came to say "still compiling the assets"
      # about a release whose nine binaries had been sitting in it for an hour.
      - name: Rendering tests skipped (release not published)
        if: steps.binary.outputs.available != 'true' && steps.release.outputs.published != 'true'
        env:
          VERSION: ${{ steps.release.outputs.version }}
          STAGE: ${{ steps.release.outputs.stage }}
          ASSETS: ${{ steps.release.outputs.assets }}
        run: |
          case "${STAGE}" in
            drafted)
              why="its release is a draft holding ${ASSETS} assets, and a draft is not downloadable -- run 'just publish-npm'"
              ;;
            building)
              why="its release is a draft with no assets yet, so build.yml is still compiling them"
              ;;
            *)
              why="no release carries it yet"
              ;;
          esac
          echo "::notice::No binary for npm-v${VERSION}: ${why}. The rendering suite did not run; re-run this job once the release is published."

      - name: Rendering tests did not run
        if: steps.binary.outputs.available != 'true' && steps.release.outputs.published == 'true'
        run: |
          echo "::error::v$(node -p "require('./package.json').version") is published but its binary could not be fetched, so the rendering suite did not run."
          exit 1

  # The one context this workflow offers a branch ruleset, and the reason the
  # jobs above are free to skip. It runs whatever they did -- `always()` -- so
  # a pull request that skips every one of them still gets a report rather than
  # a check that never arrives.
  #
  # A skipped dependency passes and a failed or cancelled one fails. The test
  # is a `case` listing what is acceptable rather than a `contains` looking for
  # what is not: an unrecognised result -- a state GitHub adds later, or an
  # empty string from a dependency that never resolved -- then fails the gate
  # instead of slipping through a check written for the states known today.
  #
  # `needs` has to name every job above it. Nothing in YAML enforces that, so
  # `just check-workflow-gates` does, and a job added without one fails that
  # gate rather than going quietly unguarded.
  ci:
    name: ci
    if: always()
    needs: [changes, test]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Report this workflow's outcome
        # The expression reaches the script through the environment rather than
        # being substituted into its text. Correct quoting is then not something
        # this step has to keep getting right: the shell never sees the
        # expression at all, so the rule that broke every one of these files --
        # an Actions expression takes single quotes only -- cannot be tripped
        # here again by an edit that looks harmless. It also keeps a value
        # GitHub interpolates out of the script body, which is the shape to
        # prefer wherever a value could ever be something other than a job
        # result.
        env:
          RESULTS: ${{ join(needs.*.result, ' ') }}
        run: |
          echo "dependency results: ${RESULTS}"
          for r in ${RESULTS}; do
            case "$r" in
              success | skipped) ;;
              *)
                echo "::error::a job this workflow covers reported '$r'"
                exit 1
                ;;
            esac
          done
          echo "every job this workflow covers succeeded or was skipped"
