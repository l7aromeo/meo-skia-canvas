.DS_Store
.vscode
.env
_arc
lib/*.node
node_modules
target

# Where every example writes when told nowhere else -- `animated_eye`,
# `feature_sheet` and `report_card`, on both surfaces. The committed copies of
# those pictures live in docs/assets/gallery and are regenerated by
# `just examples`, which names that directory explicitly.
/out/
test/assets/Oswald
test/assets/Raleway

/*.*
!/*.md
!/package.json
!/bun.lock
!/eslint.config.mjs
!/Cargo.toml
!/rustfmt.toml
!/build.rs
!/.gitignore
!/.gitattributes
!/tsconfig.json
!/.prettierrc.json
!/.prettierignore
!/.github
!/.blueprints
!/.gitmodules
.staging
.test-sandbox

# Agent scratch state. Scoped rather than a blanket `.claude/`, since committed
# skills would live under `.claude/skills/`.
containers/.claude/
.claude/audit/
CLAUDE.md
